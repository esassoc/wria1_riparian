# Build inputs that must never be committed
BID_ZID_LC_20260501.csv
TP_Split_JOIN_20260507.csv

# Local tooling state
node_modules/
__pycache__/
*.pyc
.sdd/
.claude/

# Project-internal material kept locally only: design specs, plans, audits, review
# records and the working ledger. The repo carries only what builds, tests and
# deploys the dashboard (CLIENT_DASHBOARD_NOTES.md is the operator how-to and stays).
docs/
.superpowers/
